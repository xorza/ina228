[package]
name = "ina228"
version = "0.1.0"
edition = "2024"
description = "Platform-agnostic Rust driver for the TI INA228 power/energy/charge monitor"

[dependencies]
embedded-hal = "1.0"

[dev-dependencies]
embedded-hal-mock = { version = "0.11", features = ["eh1"] }
